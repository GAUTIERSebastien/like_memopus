
const Term = (term: any) => {
  return (
    <section className="">

      <button className="btn btn-secondary">
        {term.name}
      </button>
      
    </section>
  );
}

export default Term;